# DeepSeek Harness Desktop 方案总结

> 状态：**方案 B（混合式插件壳）已实施（0.1.2）**。本文件为方案设计文档；与实现有出入处以实现为准（引擎安装方式、安装包体积等）。

## 1. 目标与定位

把 DeepSeek Harness 的使用（装环境、clone、构建、起服务、开浏览器）封装为桌面应用：

- **零依赖**：用户只需安装应用，不需要 Node/git/pnpm/SSH；引擎首次联网安装，之后离线可用
- **零配置**：开箱即用，无手动步骤；密钥在 harness 界面内自行配置
- **自动闭环**：引擎与壳各自自动更新，均无需用户动手
- **插件化**：壳以官方 Cordis 插件机制挂进 harness（服务直连 + 桌面能力 contract），支持界面内安装/管理上游插件

独立社区项目，与深度求索无隶属/背书关系；"DeepSeek Harness" 为深度求索注册商标。

## 2. 总体架构

```
┌────────────────────────────────────────────────────────────┐
│ 安装包（electron-builder 产出，~108MB）                      │
│  ├─ 应用本体（Electron：加载页 + 控制台 + 浮层注入）          │
│  ├─ 捆绑运行时：官方 Node.js 便携版（v24 LTS，自带 npm）      │
│  ├─ 捆绑 pnpm（引擎安装 + harness 插件管理依赖）              │
│  └─ 桌面壳插件（引擎首次启动联网安装，不随包分发）            │
└──────────────────────────┬─────────────────────────────────┘
       首次启动联网安装引擎到应用数据区（可写，~200MB，一次性）
                           │ spawn 捆绑 node.exe
                           ▼
┌─────────────────────────────────────────────────────────┐
│ harness 子进程（捆绑 node 运行）                          │
│  dsh web（官方 web profile）                             │
│  └─ 桌面壳插件（Cordis 插件，挂进 harness）               │
│      ├─ 服务直连：设置 / 桌面 contract                          │
│      └─ 暴露桌面 contract：profile 切换 / 窗口 / 托盘      │
└───────────────┬─────────────────────────────────────────┘
                │ loopback HTTP + WebSocket
                ▼
         Electron 壳（窗口/托盘/加载页/控制台）
```

- 应用不探测系统环境，全部使用捆绑运行时（Node 22+ 必须——harness 使用 `node:sqlite`）
- **混合式插件壳**：壳插件以官方 Cordis 插件机制挂进 harness 子进程，获得服务直连与桌面能力暴露；Electron 壳保持在外（保留崩溃隔离与实时更新）
- 服务生命周期自动管理：启动自动起、退出自动停、崩溃自动重启
- 端口被占用时复用；若占用者是其他程序，识别并提示更换端口

## 3. 用户使用流程

```
安装（~108MB，零依赖）→ 双击应用
  → 加载页（品牌 + "正在启动…"，1-3 秒）
  → 自动进入 Web 界面
  → 后台并行检查版本：有新版 → 界面右下角浮层提醒 [立即更新]
  → 首次使用 agent：界面内"设置 → 模型"添加 API key（可选步骤，看功能需求）
  → 日常：直接用；返回控制台 = 浮层按钮 / Ctrl+Shift+D / 异常时自动出现
```

## 4. 版本管理（npm 通道）

| 项 | 机制 |
| --- | --- |
| 来源 | npm 已发布包 `@deepseek-ai/dsh`（上游打 `dsh-v*` tag 时发布） |
| 通道 | `latest`（稳定）/ `next`（最新），**默认 next** |
| 版本选择 | 设置里列出全部已发布版本，可切换任意一个；或跟随通道 |
| 检查更新 | 启动时后台快速查询 registry（轻量元数据接口），秒级，不阻塞进界面 |
| 安装位置 | 应用数据区私有目录（非全局安装，免管理员、不污染系统） |
| 更新方式 | npm 安装指定版本到私有目录；**无构建** |
| 离线能力 | 首装后离线可用；仅更新/切版本需联网 |

## 5. 更新交互

- **启动只提醒不自动更**：检测到新版 → 界面内浮层横幅"发现新版本 vX.Y.Z [立即更新] [关闭]"，用户点击才更新；忽略仅本次会话隐藏
- **自动更新开关**：设置中可开（**默认关**）；开启后检测到新版后台静默更新
- **更新流程**：后台安装 → 完成自动重启服务并刷新界面（手动更新时服务在跑也自动衔接）
- **切版本**：设置中选择具体版本 → 安装并重启服务
- **壳自动更新**（electron-updater）：应用本体启动时检查 GitHub Releases，后台下载、重启时安装。两层闭环——dsh 引擎走 npm、应用壳走 Release

## 6. 界面设计

### 启动加载页
极简品牌页（logo + "正在启动…"），服务就绪自动进界面；无按钮/日志/设置元素。

### 控制台（异常出口 + 主动进入）
```
┌────────────────────────────────────────────────────────────┐
│ DSH · DeepSeek Harness Desktop      [当前 v0.1.0-rc.8]     │
│                                     [最新 v0.1.0-rc.8 ✓]   │
│                                     状态: 服务运行中 ●      │
├────────────────────────────────────────────────────────────┤
│ ⚠ 发现新版本 v0.1.0-rc.9 [立即更新] [忽略]                  │
├────────────────────────────────────────────────────────────┤
│ [立即更新]  [打开界面]  [复制诊断信息]  [打开日志文件夹]      │
│ ──────────────────────────────────────────────             │
│ ▸ 设置                                                      │
│   版本: 跟随 next ▼   端口: 3080   □ 自动更新               │
│   返回控制台快捷键: Ctrl+Shift+D    [恢复默认]              │
│ ──────────────────────────────────────────────             │
│ 运行日志 ────────── [清空]                                  │
│ …                                                          │
├────────────────────────────────────────────────────────────┤
│ 服务地址: http://127.0.0.1:3080 · 运行时: 内置 v24 ·        │
│ Ctrl+Shift+D 返回控制台                                     │
└────────────────────────────────────────────────────────────┘
```

| 区域 | 内容 | 说明 |
| --- | --- | --- |
| 头部信息区 | 当前版本 vs 最新版本、阶段状态、状态点 | 替代旧版"本地/远端 commit"徽章 |
| 更新横幅 | 提醒 + 立即更新 + 忽略 | 更新交互核心 |
| 错误横幅 | 错误信息 | 预检失败、端口占用等 |
| 按钮区 | 立即更新、打开界面、复制诊断信息、打开日志文件夹 | 原"检查更新/启动服务/停止服务"删除（自动管理覆盖） |
| 设置 | 版本选择、端口、自动更新、快捷键（可修改+恢复默认） | 原"仓库路径"删除 |
| 日志面板 | 运行日志 + 清空 | 排障 |

### Web UI 内注入浮层（主进程注入）
- 常驻右下角小部件：**返回控制台**入口（不只靠快捷键）
- 有更新时：横幅"发现新版本 vX.Y.Z [立即更新] [关闭]"，关闭后常驻入口仍在
- 注入方式：`executeJavaScript` 向 `document.body` 尾部追加独立元素，样式用 `element.style`（避开页面 CSP）；不依赖 harness 的 DOM 结构，升级兼容

### API Key
壳不处理密钥。用户直接在 harness 界面的"设置 → 模型"中添加模型/API key，写入托管凭证存储（`.credentials.yaml`）立即生效；密钥不下发浏览器（有专门测试保证）。

### 多配置（Profile）
- harness 自带 profile/agent 预设体系（web、agent-presets：code/cordis/minimal/standard）
- 壳插件暴露 profile 管理服务：列出/切换配置，每个配置可带不同模型、插件组合、工作目录
- 控制台/设置中可切换；切换即重启 harness 子进程加载新配置

## 7. 稳健性与排障

- **日志落盘**：控制台日志同步写 `userData/logs/dsh-desktop-YYYYMMDD.log`，5MB 轮转
- **复制诊断信息**：一键汇总版本/平台/配置/最近日志 → 剪贴板 → 粘贴给开发者/发 issue
- **打开日志文件夹**：控制台按钮直达日志目录
- **服务崩溃自愈**：进程意外退出（OOM、瞬时 bug）→ 自动重启（退避，3 次/短时间后停止并提示，避免无限循环）；持续崩溃时指引看日志/诊断
- **渲染进程崩溃兜底**：`render-process-gone`/`did-crash` → 弹窗提示 + 记日志
- **端口占用识别**：健康检查校验页面标志（`window.__DSH_BOOT__`），非 harness 页面提示"端口被其他程序占用，建议更换端口"
- **快捷键可配置**：默认 `Ctrl+Shift+D`，设置中修改后主进程重新注册

保留不变：内嵌窗口导航白名单（仅 localhost）、渲染健康检查（非 HTML 停回控制台）、单实例锁。

## 8. 平台、打包与签名

| 平台 | 产物 | 构建位置 | 签名 |
| --- | --- | --- | --- |
| Windows x64 | NSIS + 便携版 | 本机 / CI | 发布初期免签（README 说明"更多信息→仍要运行"）；正式分发用 OV 证书或 Azure Trusted Signing |
| Linux x64 | AppImage + deb | CI（ubuntu，原生构建） | 不需要 |
| macOS universal | dmg + zip | CI（macos runner） | Apple 证书 + 公证（未签时右键打开） |
| Android / iOS | — | Electron 不支持移动端 | ❌ 范围外 |

**CI**（`.github/workflows/build.yml` 已配置）：打 `v*` tag → 三平台矩阵构建 → 自动发布 GitHub Release（同时供 electron-updater 使用）。

**零依赖构建流程**：构建时下载 Node 便携版与 pnpm → 连同桌面壳插件一并打包（引擎不打包，首次启动联网安装）。

### 插件支持（两层）
1. **上游 harness 插件**：harness 界面内置插件管理（`settings→插件`），安装/移除走 `dsh plugin`（转调捆绑的 pnpm）——装插件能力随 harness 自带
2. **桌面能力插件**：壳插件对外暴露 contract（profile 切换/窗口/托盘），第三方插件可调用——生态潜力，需配合文档与示例

## 9. 实施状态

### 已完成（方案 B，0.1.2）
1. **桌面壳插件**（Cordis 插件）：挂进 harness web profile；暴露 profile 管理/桌面 contract
2. **主进程**：捆绑运行时解析、版本检查（registry）、安装/更新/切版本（pnpm 私有目录）、服务崩溃自愈、渲染崩溃兜底、端口占用识别
3. **渲染层**：加载页 splash、设置页、浮层注入（更新提醒 + 返回控制台）
4. **托盘驻留**：关窗驻托盘、托盘菜单（显示界面/控制台/退出）
5. **局域网访问**：设置项（默认关）→ 绑定 LAN IP + trusted-host
6. **日志系统**：落盘 + 循环轮转（5×5MB）+ 复制诊断信息 + 打开日志文件夹
7. **壳自动更新**：electron-updater（GitHub Releases 渠道，随 CI 发布）
8. **构建流程**：Node 便携版 + pnpm + 桌面壳插件打包（引擎联网安装）；CI 三平台构建
9. README/SOLUTION 同步；Windows 构建验证，CI 三平台验证

### 0.1.2 方案变更（引擎不再随包捆绑）
- 引擎改为**首次启动联网 pnpm 安装**（`--node-linker=hoisted` 平铺，避免 pnpm 11 isolated 依赖链接缺失）
- 安装包 ~108MB（不捆 `@deepseek-ai/dsh` 依赖树，此前 ~153MB）
- 新增 `verifyInstalled` 完整性校验：残缺引擎树启动时自动删除并回退，避免崩溃循环
- 新图标（`build/icon.ico` 六尺寸）；Windows exe 图标由 afterPack rcedit 钩子写入

## 10. 风险与限制

- 版本更新节奏跟随上游发布（rc 阶段发布频繁，符合"用最新"诉求）；master 未发布的 commit 不可选（按确认：只支持已发布版本）
- 安装包 ~108MB（捆绑 Node + pnpm + 桌面插件；引擎首次联网安装）
- 更新需联网（离线时用现有版本，不阻塞）
- 未签名阶段有 SmartScreen / Gatekeeper 提示；正式分发需证书（见第 8 节）
- agent 功能需要 API key，用户在 harness 界面内配置（非依赖、非自动）
- 壳插件依赖 harness 插件 API，版本升级后需回归测试（每期 rc 验证一次）

## 11. GitHub 发布准备（对比参考仓库）

### 参考仓库已有的发布资产 vs 我们

| 资产 | 参考仓库 | 我们 | 处理 |
| --- | --- | --- | --- |
| LICENSE（MIT） | ✅ | ✅ 已有 | 无需改 |
| 商标/独立声明 | ✅ README 多处 | ✅ README 已有 | 需在 CONTRIBUTING/LICENSE 首页显眼处重申 |
| 截图/封面 | ✅ hero + 界面预览 | ❌ | **实施完成后补**（加载页/控制台） |
| 徽章 | ✅ 版本/下载/星标/license/平台 | ⚠️ 已有占位 | 替换为真实仓库地址 |
| README 多语言 | ✅ 中/英 + i18n | ⚠️ 仅中文 | 补英文版（二期） |
| CONTRIBUTING | ✅ | ❌ | **新建**：构建/测试/PR 流程 |
| CODE_OF_CONDUCT | ✅ | ❌ | **新建** |
| docs/ | ✅ 用户指南/FAQ/架构/插件开发 | ⚠️ 有 SOLUTION.md | **新建** user-guide.md / faq.md / architecture.md |
| .github 模板 | ✅ issue/PR 模板 | ❌ | 新建 issue/PR 模板 + SECURITY.md |
| CHANGELOG | ✅ | ❌ | 新建 |
| THIRD_PARTY_NOTICES | ✅（捆绑分发必需） | ❌ | **新建（重要）** |
| 社区渠道 | ✅ Discord/群 | ❌ | 可选，非必需 |

### LICENSE 专项（发布前必须处理）

1. **主 LICENSE**：MIT（已有）。Copyright 用 `deepseek-harness-desktop contributors`。
2. **商标与品牌**（参考仓库的写法，必须照做）：
   - "DeepSeek Harness" 是深度求索公司的**注册商标**；README/CONTRIBUTING/LICENSE 首页均需声明"仅为准确说明兼容性与技术来源而使用"
   - 声明本项目为**独立社区项目，与深度求索无隶属、合作、授权或背书关系**
   - 仓库名与图标避免暗示官方出品
3. **第三方许可（捆绑分发，必须）**：安装包重新分发以下组件，需在 `THIRD_PARTY_NOTICES.md` 列出并附许可证文本：
   - **Node.js**（许可证含附加归属条款，重新分发二进制**必须**附带其 LICENSE）
   - Electron、pnpm、electron-builder（MIT）
   - `@deepseek-ai/dsh` 全家桶及全部 npm 依赖（MIT）
   - 生成方式：构建流程用 `license-checker` 自动收集生成
4. **署名与致谢**：参考仓库有"特别感谢上游 + Cordis + Koishi"，我们同样致谢上游 DeepSeek Harness 与 Cordis 项目。

### 其他发布准备

- 替换占位符：`package.json` 的 `homepage`、README 徽章的 `your-org` → 真实仓库地址
- 发布策略：`v0.1.0` tag → CI 三平台构建 + Release（electron-updater 同渠道）
- 隐私说明：日志本地保存、诊断信息包含哪些内容（README/FAQ 说明）
- 后续可选：English README、社区渠道、插件开发文档