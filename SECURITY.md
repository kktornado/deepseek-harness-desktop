# Security Policy

## 安全模型

本项目是一个本地桌面应用：

- 引擎与壳都在本机运行，服务只监听回环地址（除非用户显式开启"局域网访问"）
- API 密钥由 harness 自身的托管凭证存储管理（`.credentials.yaml`），不下发浏览器
- 更新链路：引擎走 npm registry 校验，壳走 GitHub Releases（electron-updater 校验发布者签名）
- 局域网访问默认关闭；开启时提示用户风险，并要求配置 trusted-host 白名单

## 报告漏洞

发现安全漏洞（远程访问绕过、密钥泄露、更新链路被劫持、权限提升等）请**不要**创建公开 issue，
通过私密渠道报告：

- 首选：GitHub 私密漏洞上报（仓库页 → Security → Report a vulnerability）
- 备用：直接联系仓库维护者（见仓库主页联系方式）

请附上：

1. 影响版本与平台
2. 复现步骤（尽量最小化）
3. 影响评估与（如可行）修复建议

## 响应时限

- 确认接收：3 个工作日内
- 严重（可导致远程代码执行 / 密钥泄露 / 更新劫持）：7 个工作日给出修复或缓解方案
- 一般：30 天内安排修复或明确不修复的理由

## 支持的版本

仅支持最新发布版本与最新主分支；历史版本不提供安全修复（单机工具，升级成本低，建议保持最新）。